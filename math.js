// const myNumber = -3;
// const output = Math.abs(myNumber);

const myNumber = 25.59;
// const output = Math.ceil(myNumber);  // dosomik er porer sonhkha habe.
// const output = Math.floor(myNumber); // dosomik er por niche jabe
// const output = Math.round(myNumber); //dosomik er kasakasi songkha hobe.

for (let i = 0; i <= 20; i++) {
    const output = Math.random() * 6;
    const rounded = Math.round(output);
    console.log(rounded);
}